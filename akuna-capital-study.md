| Phase / Timeline | Algorithmic & Systems Focus | High-Yield Problem Set (Searchable Platform Problems) | C++17/20 & Systems Language Mechanics (MCQ Prep) |
| :--- | :--- | :--- | :--- |
| **Phase 1: Days 1–7**<br>*(Sept 14–20)* | High-Speed Stream Parsing & Zero-Copy Tokenization | **1. HackerRank: Attribute Parser**<br>*Focus:* Tag/attribute hierarchy parsing, string tokenization without dynamic overhead.<br><br>**2. LeetCode #443: String Compression**<br>*Focus:* In-place contiguous buffer mutation.<br><br>**3. LeetCode #8: String to Integer (`atoi`)**<br>*Focus:* Deterministic state transitions, overflow boundaries. | **Zero-Copy Views & In-Place Types**<br>• `std::string_view` & `std::span`: Pointer + length layout, non-owning semantics, lifetime and dangling hazards.<br>• Structured bindings (`auto [x, y]`): Hidden object mechanics, capture rules in lambdas.<br>• In-place types: `std::optional<T>`, `std::variant<Types...>`, and `std::visit`.<br>• Compile-time branching: `if constexpr` branch elimination rules.<br>• *Drill:* `cppquiz.org` (Questions 1–25). |
| **Phase 2: Days 8–14**<br>*(Sept 21–27)* | Memory Layout, Cache Locality & Object Lifecycle | **1. LeetCode #622: Design Circular Queue**<br>*Focus:* Contiguous array backing, modulo wrap-around arithmetic, zero heap reallocations.<br><br>**2. LeetCode #146: LRU Cache**<br>*Focus:* Hybrid structure composing an `std::unordered_map` with an intrusive/doubly linked sequence. | **Value Categories, Move Semantics & Struct Packing**<br>• Value categories: `lvalue`, `prvalue`, `xvalue`, and universal/forwarding references (`T&&`).<br>• Move semantics: `std::move` unconditional casting, `noexcept` move constructors, and vector reallocation safety.<br>• Guaranteed copy elision (C++17): RVO vs. NRVO mechanics.<br>• Hardware alignment: `alignof`, `alignas`, 64-byte cache line padding, and false sharing prevention.<br>• *Drill:* `cppquiz.org` (Questions 26–50). |
| **Phase 3: Days 15–21**<br>*(Sept 28–Oct 4)* | Monotonic Structures & Stream Evaluation | **1. LeetCode #239: Sliding Window Maximum**<br>*Focus:* Monotonic `std::deque` maintaining descending index order.<br><br>**2. LeetCode #739: Daily Temperatures**<br>*Focus:* Monotonic index stack for next-greater element evaluation.<br><br>**3. HackerRank: Simple Text Editor**<br>*Focus:* State rollback and inverse delta command logging. | **Three-Way Comparisons, Concepts & Invariants**<br>• Spaceship operator `<=>` (C++20): `std::strong_ordering`, `std::weak_ordering`, and defaulted comparisons.<br>• Concepts and constraints (C++20): `requires` clauses, concept subsumption, and replacing `std::enable_if_t`.<br>• Comparator contracts: Enforcing **Strict Weak Ordering** (`<`) on associative containers and standard sorting algorithms.<br>• *Drill:* `cppquiz.org` (Questions 51–75). |
| **Phase 4: Days 22–29**<br>*(Oct 5–12)* | Concurrency Models & Assessment Emulation | **1. LeetCode #1114: Print in Order**<br>*Focus:* Thread synchronization, barrier mechanics, and atomic flags.<br><br>**2. Timed 60-Minute Mock Screen**<br>*Focus:* Complete 2 Medium/Hard problems under strict 60-minute caps directly in a browser interface without IDE assistance. | **Atomics, Memory Fences & Platform Execution**<br>• `std::atomic<T>`: Atomic read-modify-write operations (`fetch_add`, `compare_exchange_weak`/`strong`).<br>• Memory models: `std::memory_order_relaxed`, `acquire`, and `release` synchronization.<br>• Modern synchronization primitives (C++20): `std::latch`, `std::barrier`, and `std::atomic_flag::wait`.<br>• Fast I/O primitives: `std::cin.tie(nullptr)` and newline buffering over `std::endl`.<br>• *Drill:* `cppquiz.org` (Questions 76–100). |